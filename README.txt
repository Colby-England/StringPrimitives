Depends on libraries found at:
http://asmirvine.com/gettingStartedVS2019/Irvine.zip

This program uses a combination of macros and procedures to read signed integers, input as strings, from the user. These integers are stored in an array and their sum and average are computed. The integers along with the sum and average are then displayed back to the user, output as strings.